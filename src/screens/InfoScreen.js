import React from "react";
import { Text, View } from "react-native";
const InfoScreen = ({ navigation }) => {
	return (
		<View>
			<Text>Wanna get some info?</Text>
		</View>
	);
};

export default InfoScreen;
